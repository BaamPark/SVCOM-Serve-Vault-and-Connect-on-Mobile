VAULT_PATH=/absolute/path/to/your/obsidian/vault
APP_HOST=0.0.0.0
APP_PORT=3210
# Preferred: set a SHA-256 hash produced by `npm run hash-password -- "your-password"`
APP_PASSWORD_HASH=
# Fallback for local testing only
APP_PASSWORD=
SESSION_TTL_HOURS=24
