{
  "name": "obsidian-local-web",
  "version": "0.1.0",
  "private": true,
  "description": "Local web editor for an Obsidian vault",
  "scripts": {
    "start": "node server.js",
    "hash-password": "node server.js --hash-password"
  }
}
